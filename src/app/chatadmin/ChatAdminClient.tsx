"use client";
import { useState, type FormEvent } from "react";
import type { ChatCharacter } from "@/lib/chatAdmin";
import styles from "./chatadmin.module.css";
const empty:Partial<ChatCharacter>={display_name:"",profile_image_path:null,public_age:18,bio:"",location:"",languages:["Deutsch"],personality:"",writing_style:"",emoji_style:"sparsam",sentence_style:"kurz und natürlich",typical_phrases:[],forbidden_phrases:[],flirt_style:"respektvoll und innerhalb der definierten Grenzen",sales_rules:"kein Druck, keine falschen Versprechen",example_messages:[],status:"active"};
const lines=(value:string)=>value.split("\n").map(v=>v.trim()).filter(Boolean);
export function ChatAdminClient({initialCharacters}:{initialCharacters:ChatCharacter[]}){
 const [characters,setCharacters]=useState(initialCharacters);const [selected,setSelected]=useState<ChatCharacter|null>(initialCharacters[0]??null);const [editing,setEditing]=useState<Partial<ChatCharacter>|null>(null);const [incoming,setIncoming]=useState("");const [fan,setFan]=useState("");const [replies,setReplies]=useState<string[]>([]);const [notice,setNotice]=useState("");
 async function save(event:FormEvent<HTMLFormElement>){event.preventDefault();setNotice("");const fd=new FormData(event.currentTarget);const image=String(fd.get("profile_image_path")).trim();const payload={...editing,display_name:String(fd.get("display_name")),profile_image_path:image||null,public_age:Number(fd.get("public_age")),bio:String(fd.get("bio")),location:String(fd.get("location")),languages:lines(String(fd.get("languages"))),personality:String(fd.get("personality")),writing_style:String(fd.get("writing_style")),emoji_style:String(fd.get("emoji_style")),sentence_style:String(fd.get("sentence_style")),typical_phrases:lines(String(fd.get("typical_phrases"))),forbidden_phrases:lines(String(fd.get("forbidden_phrases"))),flirt_style:String(fd.get("flirt_style")),sales_rules:String(fd.get("sales_rules")),example_messages:lines(String(fd.get("example_messages"))),status:editing?.status??"active"};const response=await fetch("/api/chatadmin/characters",{method:editing?.id?"PATCH":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const body=await response.json();if(!response.ok){setNotice(`Speichern abgewiesen: ${body.error}`);return}const next=editing?.id?characters.map(v=>v.id===body.character.id?body.character:v):[...characters,body.character];setCharacters(next);setSelected(body.character);setEditing(null);setNotice("Charakter gespeichert.");}
 async function deactivate(character:ChatCharacter){const response=await fetch("/api/chatadmin/characters",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({...character,status:"inactive"})});if(response.ok){const {character:updated}=await response.json();setCharacters(characters.map(v=>v.id===updated.id?updated:v));setSelected(updated);}}
 async function remove(character:ChatCharacter){if(!confirm(`Charakter „${character.display_name}“ und nur dessen Character-Chatdaten löschen?`))return;const response=await fetch("/api/chatadmin/characters",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:character.id,revision:character.revision})});if(response.ok){setCharacters(characters.filter(v=>v.id!==character.id));setSelected(null);}}
 async function generate(){if(!selected)return;setNotice("Antwortvorschläge werden erzeugt …");const response=await fetch("/api/chatadmin/reply-suggestions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({character_id:selected.id,character_revision:selected.revision,incoming_message:incoming,fan_label:fan})});const body=await response.json();if(!response.ok){setNotice(`Anfrage abgewiesen: ${body.error}`);return}setReplies(body.replies);setNotice(body.safety_note);}
 return <div className={styles.layout}><section><div className={styles.sectionTitle}><div><p className={styles.eyebrow}>Charaktere</p><h2>Getrennte Personas</h2></div><button onClick={()=>setEditing(empty)}>Charakter hinzufügen</button></div><div className={styles.cards}>{characters.map(c=><article className={`${styles.card} ${selected?.id===c.id?styles.selected:""}`} key={c.id}><div className={styles.avatar}>{c.display_name.slice(0,1).toUpperCase()}</div><div><h3>{c.display_name}</h3><p>{c.public_age} · {c.status==="active"?"Aktiv":"Inaktiv"} · Revision {c.revision}</p></div><div className={styles.actions}><button onClick={()=>setSelected(c)}>Auswählen</button><button onClick={()=>setEditing(c)}>Bearbeiten</button>{c.status==="active"&&<button onClick={()=>deactivate(c)}>Deaktivieren</button>}<button className={styles.danger} onClick={()=>remove(c)}>Löschen</button></div></article>)}</div></section>{editing&&<form className={styles.editor} onSubmit={save}><h2>Character Editor</h2><fieldset><legend>Identität</legend><label>Name<input name="display_name" defaultValue={editing.display_name}/></label><label>Öffentliches Alter<input name="public_age" type="number" min="18" max="99" defaultValue={editing.public_age}/></label><label>Bio<textarea name="bio" defaultValue={editing.bio}/></label><label>Ort (optional)<input name="location" defaultValue={editing.location??""}/></label><label>Sprachen, eine pro Zeile<textarea name="languages" defaultValue={editing.languages?.join("\n")}/></label><label>Private Bildreferenz (bestehender FanMind Storage-Pfad)<input name="profile_image_path" defaultValue={editing.profile_image_path??""} placeholder="chat-characters/workspace-id/character-id/datei.jpg"/></label></fieldset><fieldset><legend>Persönlichkeit</legend><textarea name="personality" defaultValue={editing.personality}/></fieldset><fieldset><legend>Schreibstil</legend><label>Stil<textarea name="writing_style" defaultValue={editing.writing_style}/></label><label>Emoji-Stil<input name="emoji_style" defaultValue={editing.emoji_style}/></label><label>Satzstil<input name="sentence_style" defaultValue={editing.sentence_style}/></label><label>Typische Phrasen<textarea name="typical_phrases" defaultValue={editing.typical_phrases?.join("\n")}/></label><label>No-Gos<textarea name="forbidden_phrases" defaultValue={editing.forbidden_phrases?.join("\n")}/></label></fieldset><fieldset><legend>Beispiele</legend><textarea name="example_messages" defaultValue={editing.example_messages?.join("\n")}/></fieldset><fieldset><legend>Kommunikations-/Verkaufsregeln</legend><label>Flirt-/Kommunikationsstil<textarea name="flirt_style" defaultValue={editing.flirt_style}/></label><label>Verkaufsregeln<textarea name="sales_rules" defaultValue={editing.sales_rules}/></label></fieldset><div className={styles.actions}><button type="submit">Speichern</button><button type="button" onClick={()=>setEditing(null)}>Abbrechen</button></div></form>}{selected&&<section className={styles.composer}><p className={styles.eyebrow}>Manueller Copy-&-Open-Flow · {selected.display_name}</p><h2>Nachricht manuell einfügen</h2><label>Fan/Chat-Bezeichnung (optional)<input value={fan} onChange={e=>setFan(e.target.value)} maxLength={120}/></label><label>Von OnlyFans kopierte Fan-Nachricht<textarea value={incoming} onChange={e=>setIncoming(e.target.value)} maxLength={4000}/></label><button onClick={generate} disabled={selected.status!=="active"||!incoming.trim()}>Antwortvorschläge erzeugen</button>{selected.status!=="active"&&<p>Dieser Character ist deaktiviert. Neue KI-Antworten sind gesperrt.</p>}<div className={styles.replies}>{replies.map((reply,i)=><article key={i}><p>{reply}</p><button onClick={()=>navigator.clipboard.writeText(reply)}>Antwort kopieren</button></article>)}</div></section>}{notice&&<p role="status" className={styles.notice}>{notice}</p>}</div>
}
