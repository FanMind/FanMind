# FanMind staging template — placeholders only, never copy Production secrets or customer data.
FANMIND_RUNTIME_ENVIRONMENT=staging
NEXT_PUBLIC_APP_URL=https://staging.fanmind.example
NEXT_PUBLIC_SITE_URL=https://staging.fanmind.example
NEXT_PUBLIC_SUPABASE_URL=https://stagingprojectref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=replace_with_staging_anon_key
SUPABASE_SERVICE_ROLE_KEY=replace_with_staging_service_role_key
# Optional public Pixel ID. Keep empty until consent and a synthetic staging browser test are approved.
# Use a separate staging dataset; never mix synthetic staging PageViews into the Production dataset.
NEXT_PUBLIC_META_PIXEL_ID=

# Staging-only Meta test accounts. No Production customer account or token.
FACEBOOK_APP_ID=replace_with_staging_facebook_app_id
FACEBOOK_APP_SECRET=replace_with_staging_facebook_app_secret
FACEBOOK_REDIRECT_URI=https://staging.fanmind.example/api/integrations/facebook/callback
INSTAGRAM_APP_ID=replace_with_staging_instagram_app_id
INSTAGRAM_APP_SECRET=replace_with_staging_instagram_app_secret
INSTAGRAM_REDIRECT_URI=https://staging.fanmind.example/api/integrations/instagram/callback
FANMIND_TOKEN_ENCRYPTION_KEY=replace_with_staging_32_byte_base64_or_hex_key

# Required comparison values for any write-enabled test.
# The target ref must exactly equal the project subdomain in NEXT_PUBLIC_SUPABASE_URL.
FANMIND_TARGET_SUPABASE_PROJECT_REF=stagingprojectref
FANMIND_PRODUCTION_SUPABASE_PROJECT_REF=productionprojectref
FANMIND_ENABLE_NON_PRODUCTION_WRITES=false
FANMIND_NON_PRODUCTION_WRITE_ACK=

# Staging uses Stripe test mode only.
# Prefer a restricted rk_test_ key with only Checkout Sessions/Subscriptions/
# Coupons write plus Prices/Invoices read. A standard sk_test_ key is accepted
# only for compatibility while the restricted key is being provisioned.
STRIPE_SECRET_KEY=rk_test_replace_with_staging_restricted_key
STRIPE_WEBHOOK_SECRET=whsec_replace_with_staging_secret
STRIPE_PRICE_STARTER_SETUP=price_replace_with_staging_starter_setup
STRIPE_PRICE_STARTER_MONTHLY=price_replace_with_staging_starter_monthly
STRIPE_PRICE_INTERNAL_DAILY_TEST=price_replace_with_staging_daily_test
STRIPE_PRICE_AI_PLUS=price_replace_with_staging_plus
STRIPE_PRICE_AI_ULTRA=price_replace_with_staging_ultra
FANMIND_TAX_MODE=stripe_tax
FANMIND_STRIPE_TAX_REGISTRATION_CONFIRMED=false
# Controlled ledger cutover only. true blocks new Checkout sessions and makes
# legacy signed webhook billing writes retryable instead of acknowledging them.
FANMIND_STRIPE_BILLING_WRITE_FREEZE=false
FANMIND_ENABLE_REFERRAL_BILLING=false
FANMIND_REFERRAL_SANDBOX_ACK=
FANMIND_REFERRAL_RECONCILE_SECRET=replace_with_staging_random_secret

# Public/demo/automation switches stay off until each staging test is approved.
FANMIND_PUBLIC_DEMO_ENABLED=false
FANMIND_ENABLE_TELEGRAM_SEND=false
FANMIND_OPERATIONS_MONITOR_ENABLED=false
FANMIND_OPERATIONS_EMAIL_ENABLED=false
FANMIND_SERVER_ERROR_TRACKING_ENABLED=false
FANMIND_SERVER_ERROR_EMAIL_ENABLED=false
FANMIND_ENABLE_ISOLATED_RELEASE_DEPLOY=false

# Use synthetic test users and contacts only. Provisioning injects the real
# list from the protected GitHub variable FANMIND_STAGING_ADMIN_EMAILS.
FANMIND_ADMIN_EMAILS=staging-admin@example.com
FANMIND_STAGING_E2E_WORKSPACE_ID=
FANMIND_STAGING_E2E_CONTACT_ID=
FANMIND_STAGING_E2E_SECONDARY_WORKSPACE_ID=
FANMIND_STAGING_E2E_SECONDARY_CONTACT_ID=
# Core/CSV never stores a member password here. Its protected hosted job
# generates, masks, activates and revokes that credential ephemerally.
OPENAI_API_KEY=replace_with_staging_limited_key
# The public Staging app remains mail-disabled. Provider acceptance uses a
# separate protected workflow and never copies its key into this runtime.
RESEND_API_KEY=
FANMIND_NOTIFICATION_FROM=

# Manual AI-tier acceptance only. Keep unset outside the short, approved run.
FANMIND_AI_TIER_ENTITLEMENT_MIGRATION_CONFIRM=
FANMIND_AI_TIER_STAGING_ACCEPTANCE_CONFIRM=
FANMIND_AI_TIER_STAGING_WORKSPACE_ID=
FANMIND_TARGET_DB_HOST=
PGHOST=
PGPORT=5432
PGDATABASE=postgres
PGUSER=postgres
PGPASSFILE=
