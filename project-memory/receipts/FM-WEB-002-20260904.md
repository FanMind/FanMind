# Execution Receipt — FM-WEB-002

- Date: 2026-09-04 Europe/Vienna
- Status: IMPLEMENTED_NOT_VERIFIED
- Risk: R3
- Change: FM-CR-015; continues FM-WEB-001/FM-CR-014
- External mutation: none

## Result

The repository now contains three separate exact-main controls for the dormant
Website Chat handoff schema: read-only verification, independently confirmed
isolated-Staging apply and rollback-only synthetic acceptance. The shared
policy binds the exact reviewed commit to `main`, the Staging app origin,
Supabase project reference and direct PostgreSQL host while rejecting every
matching Production target, indirect libpq target and TLS mode other than
`verify-full`.

The verification runner snapshots a private no-follow `0600` password file,
removes alternate connection variables and independently checks RLS, the
absence of policies and browser/PUBLIC grants, exact service-role privileges,
function security/search-path shape and the revoked legacy ingestion RPC.

The acceptance uses deterministic synthetic identifiers inside one explicitly
selected Staging Workspace. It proves real database denial for `anon` and
`authenticated`, temporarily opens only the canonical processing entitlement,
requires the established synthetic/processing-acceptance markers, creates a
transaction-scoped fixture path, verifies message and handoff idempotency,
rejects a handoff without a prior message and a wrong origin for both RPCs,
checks the CRM contact/conversation, purpose-bound fingerprint and absence of
outbound Website Chat messages, then rolls the whole transaction back. A
second read-only transaction proves that no synthetic fixture remains.

No workflow was dispatched, no database was changed, no installation was
activated, and neither AI nor email delivery was invoked.

## Verification

- Controlled SQL SHA-256:
  `44c77eae690b1ac53e8a30ae0fb890232f61f493d3f808ee4044a70a5d3f7744`
- Website Chat: 30/30 PASS
- Release integrations: 78/78 PASS
- Operations: 1113/1113 PASS after synchronizing the intentional workflow
  topology count
- Action pinning and product truth: PASS
- Project Memory quality/status: PASS
- Drift baseline reconciled to the reviewed Source-of-Truth change first
  published in `6452cb2452b3e5a664d86f5073a410f3744b1bae`
- Next type generation and TypeScript: PASS
- ESLint: PASS with one pre-existing unrelated Mobile warning
- Next production build: PASS

## Open boundary

Exact-main remote checks remain pending at this repository checkpoint. Every
protected Staging dispatch is still a separate action: verify needs explicit
dispatch, apply needs its own authorization, and rollback-only acceptance needs
another authorization. Production, real visitor data, installation activation,
AI, email verification and outbound delivery remain outside this task.
