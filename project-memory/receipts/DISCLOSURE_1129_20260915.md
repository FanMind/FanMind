# PDF disclosure correction #1129 — source receipt

- Task: FM-META-001 / issue #1129 (existing profile/export #514).
- Risk: R3; personal-data completeness and tenant authorization.
- Work: LOCK-FM-DISCLOSURE-1129-20260915, IN_PROGRESS for review/acceptance; owner explicitly requested this correction. Start recorded in #1129 comment 5686123457.
- Source status: IMPLEMENTED_NOT_VERIFIED for full repository/runtime acceptance. No production-success claim.

## Preflight and reconciliation

Mandatory Project Memory and relevant canonical/source readers were read before implementation. The original baseline was e7208cd1abcde8a881cc23ae4b546cbcd6278c7f. While this work was running, #1128 was merged by another actor as 5114e9a0e4d2ee54ca8904ef0eb2dd2cf0821bac. The complete commit comparison shows no changes to this correction's six application/test files; this branch is therefore based on the newer main/tree 3ef409eb1e4a99cb4f7f89d1c2f787703cb357eb and preserves its Daily changes. This session did not merge #1128. A merge is not a deployment or green-CI claim.

Fresh read-only pg_catalog/to_regclass verification of the existing FanMind Production target found the nine optional Content/Creator/analysis/Social-provider tables absent. No customer rows were read and no SQL mutation occurred. Historical Creator/PDF Staging acceptance remains valid for its original target and scope; it is not acceptance of this Production schema shape. The installed Production release and authenticated browser download remain unverified in this session.

A runtime PGRST205 proves API-schema unavailability, not physical absence or zero rows. This correction consequently emits an explicitly incomplete PDF (Teilauskunft / Partial export) rather than silently declaring such categories empty. A current read-only catalog observation is not hard-coded into application behavior.

## Bounded implementation

- Keep the existing authorized account, Workspace, contact and stored-data projections.
- Treat content_sources as an optional module. Only structured table-bound 404/PGRST205 or 42P01 produces an explicit unavailable section; generic 404, denied access, malformed response, network/server error and exhausted column projections still abort.
- Never suppress a failed later pagination page. Preserve existing legacy select variants and actual empty datasets.
- The existing six-field privileged Social metadata reader remains owner-rechecked and allowlisted; an unavailable table requires an explicit status consumer instead of returning an unqualified empty result. No broader service-role fallback or RLS change.
- Partial status appears in PDF title, before account content, in each unavailable category, in the filename and response header. Unavailable sections do not show zero-record counts. Successfully read data remains included.
- Any failed auth/Workspace/data/PDF step has fixed localized HTML with retry/back links, no reflected diagnostics or personal data, private no-store, nosniff and restrictive CSP. No new client script or external dependency.
- Source rollback is a bounded revert of these files, without data/schema rollback.

## Implementation evidence

The five complete original files were reconstructed through the connected GitHub API and checked against their Git blob hashes before editing. All six resulting source/test/CSS blobs match the locally inspected files exactly.

Local command: node --test --test-skip-pattern='real PDF' tests/social-connection-disclosure.test.mjs
Result: 40 behavioral tests passed, zero failures. This is explicitly a filtered local run. The two real PDF-engine tests were not executed successfully locally because pdfnative is unavailable in this restricted container. They are unconditional in the existing CI-registered suite and must pass with the repository's actual dependency before acceptance. Four changed TypeScript files transpile without diagnostics; this is not a full repository typecheck/build claim.

Static DE/EN HTML+CSS rendering at 1280x900 and 390x844 passed layout/visible-controls/no-horizontal-overflow assertions. HTTP/browser navigation was blocked by the container browser policy, so this static rendering is not a browser-download, CSP-network or authenticated-runtime acceptance. No restriction was disabled.

## Still open / exact next step

Inspect current-head CI and independent review, including the two actual PDF tests and existing disclosure regression suite. Render/inspect actual generated PDFs and perform a real browser download on an authorized test target. Reconcile any unrelated baseline failures without suppressing tests or modifying Daily/Mobile in this PR. Before merge, integrate this tracked work/status into the existing STARTED_WORK, WORK_LOCKS, TASK_LEDGER and handoff entries using complete files in a normal checkout; the connector's large truncated previews were deliberately not written back. This receipt and #1129 retain the current task/lock/evidence meanwhile.

Only after approved normal release and exact /api/version proof may the owner be asked to retry the existing account's PDF button and Production be marked confirmed. No migration, provider/Creator/Meta activation, payment, deletion, credential change or production deployment is part of this source receipt. Issue #1129 stays open until the applicable acceptance is complete.
