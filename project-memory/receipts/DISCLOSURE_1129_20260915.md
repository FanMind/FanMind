# PDF disclosure correction #1129 — source receipt

- Task: FM-META-001 / issue #1129 (existing profile/export #514).
- Risk: R3; personal-data completeness and tenant authorization.
- Work: LOCK-FM-DISCLOSURE-1129-20260915, IN_PROGRESS until reviewed merge/deploy/live browser acceptance; owner explicitly requires all data of the signed-in Creator account and its own Workspace.
- Current source: PR #1130, branch `fix/pdf-data-disclosure-1129-20260915`.
- No Production-success claim until exact-head CI/review, merge, normal deploy, `/api/version` and owner browser evidence exist.

## Reconciled contract

Mandatory Project Memory/canonical readers and current GitHub main were read. Baseline main is `5114e9a0e4d2ee54ca8904ef0eb2dd2cf0821bac` (#1128). One Creator = one account = one Workspace remains binding. The earlier #1129 partial-export design is superseded: a successful disclosure is complete-only; any required read/page/authorization/PDF failure returns the safe error page and no incomplete PDF.

A fresh read-only FanMind Production catalog check was performed during this work. Current Production physically contains the browser-readable account/Workspace families used by the collector: `profiles`, `workspace_members`, `workspaces`, `contacts`, `memories`, `followups`, `conversations`, `conversation_messages`, `conversation_summaries`, `contact_reply_targets`, `fan_analysis_reports`, `contact_ai_profiles`, `workspace_voice_profiles`, `ai_usage_events`, `social_connections` and `meta_webhook_events`. Their RLS/policies provide the expected own-account/Workspace SELECT boundaries. No customer rows were read by this catalog/policy check.

The same current Production check proves `workspace_ai_prompt_settings`, Content/metric/conversation-analysis and Creator-foundation tables are not currently installed Production storage. They are not invented as stored Production data. If a new table becomes Production storage later, the disclosure inventory must be extended in the same release.

Current Production also contains owner-related service-only data families. The disclosure therefore includes safe, exact-owner projections for matching `pilot_inquiries`, `referral_program_members`, own referrer/referred relationships, `referral_discount_snapshots` and `account_deletion_requests`. Referral counterparty user/Workspace identifiers, admin-only notes and provider credentials are not disclosed.

## Implementation boundary

- Route is Owner-only: a Workspace member cannot export the Creator Workspace.
- Identity, profile, own Membership, Workspace/master/contract/billing data, full contact rows, Memories, Follow-ups, Conversations, all-channel Messages, Summaries, Reply Targets, Fan/AI profiles, writing-style profiles, AI usage, Social metadata and stored Meta webhook events are exported from the exact authorized Creator/Workspace.
- Account `user_metadata` and Workspace role are included. Existing human-readable contacts remain, plus full contact rows so IDs/current safe fields are not lost.
- Active browser-readable rows use complete safe row reads with strict user/Workspace validation and full pagination. A later-page failure aborts instead of truncating.
- Service-only own-account rows require a second server-side `requireAuthorizedWorkspace` owner check before and after the reads, exact identity/workspace/email filters and bounded pagination.
- Passwords, API/access/refresh tokens, ciphertext/secrets and Stripe provider identifiers are excluded. Referral relationships remove counterparty identifiers.
- Success is only normal filename + `X-FanMind-Disclosure-Status: complete`. No `partial`/`Teilauskunft` success remains.
- Error UI is fixed localized DE/EN with retry/back, no raw diagnostics/personal content, private no-store, nosniff, no-referrer and restrictive CSP.
- No database migration, RLS/grant change, Daily/Billing activation, provider/Creator activation, payment, deletion or credential mutation is part of this fix.

## Verification contract

The CI-registered disclosure suite covers current Production-family enumeration, account/Membership/Workspace binding, Owner-only behavior, all-channel Messages, safe-field preservation, secret/provider-id redaction, service-only own-data boundaries, referral counterparty redaction, foreign-row rejection, missing/denied/malformed/network failures, 501-row pagination and failed later page, complete-only transport, DE/EN safe error pages, locale injection and real multi-page PDF creation with `pdfnative`.

Only the exact final head may support merge. Required before merge: FanMind CI, Browser E2E, CodeQL, language and Project Memory workflows green plus independent Codex review completed without unresolved findings. After merge: normal Production deployment, exact public `/api/version` binding and existing-account browser download. Issue #1129 and this lock remain open until that live evidence.
