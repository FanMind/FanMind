# FM-CREATOR-001 — confirmed-chat controlled rollout runner — 2026-09-23

- Status: IN_PROGRESS
- Risk: R4
- Work lock: `LOCK-FM-CREATOR-CONFIRMED-CHAT-ROLLOUT-RUNNER-20260923`
- Holder: autonomous FanMind Builder
- Baseline: exact `main` `832ceac28265528c412efb36a2eca90bd2514a65`; PR #1167 is merged and its repository-only account/contact deletion-verification scope is consumed.
- Task / NBA: `FM-CREATOR-001` / `NBA-CREATOR-INTELLIGENCE`.
- Contracts / gates: `FM-CONTRACT-CREATOR-AI-001`, `FM-CONTRACT-DISCLOSURE-DELETE-001`, `FM-IGATE-CREATOR-AI-001`, `FM-IGATE-DISCLOSURE-DELETE-001`.
- Scope: repository-only controlled migration runner/checksum and target-bound read-only schema VERIFY contract for `supabase/controlled/20260923023000_creator_confirmed_chat_learning.sql`. Pin the exact reviewed SQL artifact, reject partial/drifted schema, prove relevant ACL/RLS/function/trigger boundaries, bind target identity and reviewed checkout, and make any future APPLY structurally staging-only and impossible while the source rollout state is still `preinstall`.
- Negative boundary: no workflow dispatch, no Staging/Production SQL, no source rollout-state switch, no feature-flag activation, no provider call, no real customer/account/contact mutation, no Billing/Stripe/Tax, no Restore write and no Mobile work in this scope.
- Ordering invariant: `preinstall` source state -> reviewed later state-switch/deploy of fail-closed readers -> target-bound read-only VERIFY -> separate owner/protected-environment authorization -> controlled Staging APPLY -> independent postflight/negative authorization evidence -> separate runtime acceptance/activation. Merge alone is never activation.
- Earlier review reconciliation: the required independent review of head `2c5bc2691a0c8fc57d3b349dc564f0cc76b084eb` found five P1 and one P2 in the verification contract: brittle stored-function matching, incomplete manual-send preservation verification, incomplete learning-table column/default verification, insufficient Supabase pooler binding, incomplete outcome-evidence verification and non-exact SECURITY DEFINER search-path verification. Those findings were corrected on this PR before head `2362d22e532f75535fe7feae7f8da942c621d716`.
- Prior required-review reconciliation: the independent review of exact head `2362d22e532f75535fe7feae7f8da942c621d716` found four P1 and one P2: foundation authorization helpers were existence-only; an empty CHECK-constraint aggregate could be NULL and bypass the cardinality test; callable PostgreSQL function metadata was incomplete; inherited `GIT_DIR`/`GIT_WORK_TREE`-style variables could redirect Git attestation; and browser roles were not explicitly rejected when BYPASSRLS/superuser semantics would defeat RLS. Those findings were corrected before head `81fc1023d7ace7ac44fa9fad9463889d9093a949`.
- Prior required-review reconciliation: the independent review of exact head `81fc1023d7ace7ac44fa9fad9463889d9093a949` found one P1 and three P2: a later competing BEFORE trigger could rewrite the manual-send provenance marker; helper EXECUTE ACLs were not checked; function-owner drift was not bound to the reviewed `postgres` owner; and an extra standalone UNIQUE index could introduce behavior-changing write constraints. Those findings were corrected before head `3e0486ab1d3171f617f96c3124fb1313586e54ce`.
- Current required-review reconciliation: the independent review of exact head `3e0486ab1d3171f617f96c3124fb1313586e54ce` found three P1 and one P2, all within this bounded verifier: a later enabled BEFORE INSERT/UPDATE trigger could still change provenance inputs after the stamp trigger without mentioning the marker; a ready/live standalone UNIQUE index could still enforce uniqueness while `indisvalid=false`; CHECK comparison erased all parentheses and therefore could not detect semantics-changing regrouping; and Git status could hide executed/control-file edits behind assume-unchanged or skip-worktree index flags.
- Current correction: reject every other enabled row-level BEFORE INSERT/UPDATE trigger that sorts after the canonical stamp trigger, while retaining direct competing-marker detection; reject standalone UNIQUE indexes whenever they are ready/live regardless of `indisvalid`; compare installed CHECK expressions against PostgreSQL-parser-canonicalized expected expressions with grouping preserved instead of deleting parentheses; and require every executed/control file to have a normal `H` index state and byte-for-byte match the exact reviewed-commit blob before target access. Existing body/owner/ACL/metadata/RLS/browser/target/TLS/rollout-state controls remain retained.
- Negative/regression proof: focused source regressions assert later-trigger rejection, ready/live invalid-unique rejection, parser-preserved CHECK grouping and reviewed-control-file/index-flag binding, in addition to the earlier owner/helper/metadata/RLS/Git-redirection/target/Production-denial cases. Current-head GitHub CI and the next single required independent review are authoritative; no runtime database action is part of this correction.
- Manager safety: this R4 correction remains the selected SAFE READY repository task. PR #1169 is intentionally serialized until this R4 verifier converges because the manager PR touches global Project-Memory/selection semantics that govern how this continuation is represented; no artificial second or third worker is created.
- Recovery: repository revert only. A later indeterminate database apply must stop for read-only verification; never blindly retry, drop or repair schema.
- Falsifier: any helper/source/blob/body/owner/ACL/metadata mismatch, later or marker-writing provenance trigger, ready/live standalone behavior-changing UNIQUE index, semantics-changing CHECK regrouping, suppressed-index or reviewed-control-file mismatch, NULL/partial constraint state, browser RLS-bypass capability, redirected Git attestation, checksum/blob mismatch, migration contract drift, partial target state, target/ref/host/TLS/checkout mismatch, Production apply attempt, preinstall apply attempt, unexpected installed target while source is preinstall, red current-head gate, unresolved P1/P2 or blocking review thread.
- Exact next: consume current-head CI and exactly one fresh required independent review of the materially new PR head. Merge immediately only if all required checks are terminal green, P1/P2 are zero, no blocking threads remain and GitHub still reports the PR mergeable. Do not dispatch protected VERIFY/APPLY from this repository-preparation scope.
